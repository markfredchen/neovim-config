require("bufferline").setup({
  options = {
    mode = "buffers",
    numbers = "none",
    offsets = {
      {
        filetype = "NvimTree",
        text = "",
        separator = false,
      }
    }
  }
})

